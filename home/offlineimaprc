[general]
accounts        = gmail
pythonfile      = ~/.offlineimap/extra.py
ui              = ttyui
maxsyncaccounts = 1

[Account gmail]
localrepository  = gmail-local
remoterepository = gmail-remote
autorefresh      = 0.5
quick            = 10

[Repository gmail-local]
type         = Maildir
localfolders = ~/mail/gmail
nametrans    = lambda f: {"archive": "[Gmail]/All Mail",
                          "drafts": "[Gmail]/Drafts",
                          "important": "[Gmail]/Important",
                          "sent": "[Gmail]/Sent Mail",
                          "spam": "[Gmail]/Spam",
                          "starred": "[Gmail]/Starred",
                          "trash": "[Gmail]/Trash"}.get(f, f)

[Repository gmail-remote]
keepalive          = 60
holdconnectionopen = yes
type               = Gmail
auth_mechanisms    = LOGIN
remoteuser         = plotnikovanton@gmail.com
remotepasseval     = get_pass("gmail")
sslcacertfile      = /etc/ssl/certs/ca-certificates.crt
nametrans    = lambda f: {"[Gmail]/All Mail": "archive",
                          "[Gmail]/Drafts": "drafts",
                          "[Gmail]/Important": "important",
                          "[Gmail]/Sent Mail": "sent",
                          "[Gmail]/Spam": "spam",
                          "[Gmail]/Starred": "starred",
                          "[Gmail]/Trash": "trash"}.get(f, f)

[Account tsystems]
localrepository  = tsystems-local
remoterepository = tsystems-remote

[Repository tsystems-local]
type         = Maildir
localfolders = ~/mail/tsystems

[Repository tsystems-remote]
type               = IMAP
remotehost         = kaliwe.ru
remoteport         = 1143
remoteuser         = aplotnik@t-systems.ru
remotepasseval     = get_pass("t-systems")
folderfilter       = lambda f: not f.startswith('Sync')

